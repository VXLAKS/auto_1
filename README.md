# 💳 Credit Scoring Model (MLOps Pipeline)

Данный проект представляет собой промышленный прототип системы кредитного скоринга. Основная цель — автоматизировать весь жизненный цикл модели: от первичной обработки данных и их строгой валидации до деплоя в Docker-контейнер с мониторингом дрифта.

---

## 🛠 Стек технологий

* **Data Engineering:** `DVC` (Data Version Control) для управления версиями данных и воспроизводимости пайплайнов.
* **Data Validation:** `Pandera` для декларативной проверки типов, диапазонов и логической целостности входных данных.
* **Modeling:** `Scikit-learn` (Pipelines), автоматический подбор гиперпараметров через `GridSearchCV`.
* **Experiment Tracking:** `MLflow` для регистрации всех запусков, логирования метрик (AUC, F1), параметров и сохранения артефактов (ROC-кривые).
* **Serving:** `FastAPI` (REST API) для предоставления интерфейса предсказаний.
* **DevOps:** `Docker` для контейнеризации и `GitHub Actions` для непрерывной интеграции (CI).

---

## 📂 Структура проекта

```text
├── .github/workflows/  # CI/CD пайплайны (GitHub Actions)
├── data/               # Данные (версионируются через DVC)
│   ├── raw/            # Исходный датасет UCI Credit Card
│   └── processed/      # Очищенные данные для обучения
├── models/             # Сохраненные веса модели (.pkl)
├── src/                # Исходный код
│   ├── api/            # FastAPI приложение
│   ├── data/           # Скрипты подготовки и валидации (Pandera)
│   ├── models/         # Скрипты обучения и логики препроцессинга
│   └── monitoring.py   # Скрипт расчета PSI и анализа дрифта
├── tests/              # Unit-тесты для API и модели
├── Dockerfile          # Инструкция по сборке образа
├── dvc.yaml            # Описание этапов пайплайна
└── requirements.txt    # Зависимости проекта

```

---

## 🚀 Как запустить проект

### 1. Подготовка окружения

```bash
# Клонирование репозитория
git clone <your-repo-link>
cd credit-scoring-model

# Создание и активация venv
python -m venv venv
source venv/bin/activate  # для Windows: venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

```

### 2. Запуск ML-пайплайна

Вместо ручного запуска скриптов используйте DVC. Он сам проверит зависимости и запустит этапы в нужном порядке:

```bash
dvc repro

```

*Эта команда последовательно выполнит: `prepare` -> `validate` -> `train`.*

### 3. Просмотр результатов в MLflow

Для анализа метрик и просмотра ROC-кривой запустите интерфейс:

```bash
mlflow ui

```

Результаты будут доступны по адресу: `http://127.0.0.1:5000`

### 4. Деплой API

**Локально:**

```bash
python src/api/app.py

```

Интерактивная документация (Swagger): `http://127.0.0.1:8000/docs`

**Через Docker:**

```bash
docker build -t scoring-app .
docker run -p 8000:8000 scoring-app

```

---

## 📈 Мониторинг и проверка дрифта

В проект встроен механизм контроля стабильности популяции (PSI). Чтобы проверить, не "протухла" ли модель на новых данных, выполните:

```bash
python src/monitoring.py

```

---

## 🤖 CI/CD

При каждом `git push` в репозиторий запускается процесс:

1. Проверка синтаксиса (Linting).
2. Запуск Unit-тестов.
3. Тестовая сборка Docker-образа.

```